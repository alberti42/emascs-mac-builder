# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor cruft
*~
\#*\#
.\#*
*.swp

# Local overrides / scratch
*.local
/tmp/

# Generated icon catalog -- assets/ holds loose source only; build.sh compiles
# the .car at build time (into the deployed .app, not the repo).
assets/**/Assets.car
